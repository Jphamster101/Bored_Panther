extends layout.pug
block whatever
    h1=message
    p=e_id
    p=hourlyWage
    p=phoneNumber
    p=certifications
    #again
        a(href='http://localhost:3000/')
            button#send Search Again?